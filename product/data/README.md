# product/data/

Product-owned data assets live here.

This includes track outline JSON files and other data consumed by the browser app or development tools as product inputs. Generated local outputs belong in `var/`, not here.
