# docs/specs/

Stable specifications live here.

A spec describes the desired end state and constraints for a product, repo, or tooling change. Mission plans in `work/` should link to the relevant spec instead of duplicating it.
