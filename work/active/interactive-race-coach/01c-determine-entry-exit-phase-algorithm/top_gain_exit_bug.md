# Bug: top_gain exit issue

There is one more bug in the current `compare_laps()` output related to
how `exit` (or `exit_brake` / `exit_throttle`) phases appear in
`top_gains`. Ask Vasco what the bug is before investigating.