# Learnings: 06-docs-and-work-archive

- The root `specs/` folder contained stable specs and now fits naturally under `docs/specs/`; keeping `docs/specs/README.md` as the orientation point made the move straightforward.
- The old root `archive/` folder only contained historical plan/handoff documents, so its contents were moved into `work/archived-plans/` and the empty folder was removed.
- `NEXT_STEPS.md` already had unrelated local edits before this slice and was left in place rather than mixing active backlog decisions into the historical archive move.
