# product/web/

Browser comparison app source lives here.

`compare.html` loads CSS from `css/` and JavaScript modules from `js/` during development. The build bundles this app into `product/dist/compare.html` for standalone `file://` use.
