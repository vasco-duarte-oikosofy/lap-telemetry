# Phase 00-layout — your task

You are working on subphase **00-layout** of the track-heatmap spec.
Open `/Users/vasco/Dropbox/GitHub/lap-telemetry/track-heatmap-spec.md` and locate the section for phase 00-layout.
Implement ONLY that section.

Standing rules (XP working agreements, file architecture, required
artifacts, things you must not do) are in `AGENTS.md` at the repo
root. Pi has already loaded them. Follow them.

## Branch

Work directly on `main`.
Make small, green commits on `main`.

## When you are done

Write `phases/00-layout/learnings.md` and `phases/00-layout/handoff.md`,
verify `npm test` is green, then exit. The orchestrator will run
the test suite and the file-size check itself — your exit + the
artifacts on disk are the signal of completion.

If anything is ambiguous, write the question to
`phases/00-layout/QUESTION.md` and exit. The orchestrator will halt
and surface it to a human.
