# scripts/

Compatibility wrappers live here.

Development script implementations have moved to `dev/scripts/`. Keep wrappers here only for stable commands that existing docs or automation still call, such as `bash scripts/test-summary.sh` and `npm run build` via `scripts/bundle.js`.
